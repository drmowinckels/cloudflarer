% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-tunnels.R
\name{cf_list_tunnels}
\alias{cf_list_tunnels}
\title{List Cloudflare Tunnels}
\usage{
cf_list_tunnels(
  account_id,
  is_deleted = FALSE,
  per_page = 50,
  max_pages = Inf,
  as_df = TRUE,
  token = NULL,
  email = NULL,
  api_key = NULL
)
}
\arguments{
\item{account_id}{Character. Cloudflare account identifier.}

\item{is_deleted}{Logical. When \code{TRUE}, include deleted
tunnels.}

\item{per_page, max_pages}{Pagination controls, see
\code{\link[=cf_collect]{cf_collect()}}.}

\item{as_df}{Logical. When \code{TRUE} (the default), returns a
data.frame via \code{\link[=cf_records_to_df]{cf_records_to_df()}}.}

\item{token}{Character. An API token. If \code{NULL} (the default),
the value of \code{Sys.getenv("CLOUDFLARE_API_TOKEN")} is used.}

\item{email}{Character. Account email. If \code{NULL} (the default),
reads from the \code{CLOUDFLARE_EMAIL} environment variable.}

\item{api_key}{Character. The Global API Key. If \code{NULL} (the
default), reads from the \code{CLOUDFLARE_API_KEY} environment
variable.}
}
\value{
A data.frame of tunnel records (or list when
\code{as_df = FALSE}).
}
\description{
Returns the cloudflared tunnels (Zero Trust) configured in the
supplied account.
}
\examples{
\dontrun{
cf_list_tunnels("abc123")
}
}
\seealso{
Other tunnels: 
\code{\link{cf_get_tunnel}()},
\code{\link{cf_list_tunnel_connections}()}
}
\concept{tunnels}
