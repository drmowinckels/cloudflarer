% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-request.R
\name{cf_resp}
\alias{cf_resp}
\title{Unwrap the result from a Cloudflare response}
\usage{
cf_resp(resp)
}
\arguments{
\item{resp}{An \code{httr2_response}, typically from
\code{cf_request(...) |> httr2::req_perform()}.}
}
\value{
The parsed \code{result} element from the response. For
single-resource endpoints this is a named list; for collection
endpoints it is a list of records.
}
\description{
Validates the standard Cloudflare envelope on a performed
response and returns its \code{result} payload. Typically the terminal
step of a request pipeline built with \code{\link[=cf_request]{cf_request()}} and
\code{\link[httr2:req_perform]{httr2::req_perform()}}.
}
\details{
The Cloudflare API wraps every response in an envelope with
\code{success}, \code{errors}, \code{messages}, \code{result}, and (for paginated
endpoints) \code{result_info}. API-level failures -- both HTTP error
statuses and envelope errors (HTTP 200 with \code{success: false}) --
raise a classed condition you can catch with
\code{tryCatch(..., cloudflarer_error = ...)}. Transport-level failures
that never reach the API (DNS resolution, connection refused,
timeout) surface as the underlying \code{httr2} error instead.
}
\examples{
\dontshow{if (requireNamespace("vcr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontshow{
Sys.setenv(CLOUDFLARE_API_TOKEN = "cloudflarer-example")
vcr::insert_example_cassette("cf_resp", package = "cloudflarer")
}
cf_request("user") |>
  httr2::req_perform() |>
  cf_resp()
\dontshow{vcr::eject_cassette()}
\dontshow{\}) # examplesIf}
}
\seealso{
Other requests: 
\code{\link{cf_collect}()},
\code{\link{cf_request}()}
}
\concept{requests}
