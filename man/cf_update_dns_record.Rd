% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-dns-records.R
\name{cf_update_dns_record}
\alias{cf_update_dns_record}
\title{Update a DNS record}
\usage{
cf_update_dns_record(
  zone_id,
  record_id,
  type = NULL,
  name = NULL,
  content = NULL,
  ttl = NULL,
  proxied = NULL,
  priority = NULL,
  comment = NULL,
  ...,
  token = NULL,
  email = NULL,
  api_key = NULL
)
}
\arguments{
\item{zone_id}{Character. Cloudflare zone identifier.}

\item{record_id}{Character. DNS record identifier.}

\item{type}{Record type: \code{"A"}, \code{"AAAA"}, \code{"CNAME"}, \code{"MX"},
\code{"TXT"}, \code{"SRV"}, etc.}

\item{name}{Record name (host). For the apex, pass \code{"@"} or the
zone name.}

\item{content}{Record content. For \code{A}/\code{AAAA} an IP address; for
\code{CNAME} a hostname; for \code{TXT} the string value.}

\item{ttl}{Time to live in seconds. \code{1} means "automatic"
(Cloudflare's default).}

\item{proxied}{Logical. Whether the record is proxied through
Cloudflare. Applies to A/AAAA/CNAME.}

\item{priority}{Integer. Required for \code{MX} records.}

\item{comment}{Optional human-readable comment.}

\item{...}{Additional fields forwarded to the API for record
types with extra requirements (\code{data} for \code{SRV}, etc.).}

\item{token}{Character. An API token. If \code{NULL} (the default),
the value of \code{Sys.getenv("CLOUDFLARE_API_TOKEN")} is used.}

\item{email}{Character. Account email. If \code{NULL} (the default),
reads from the \code{CLOUDFLARE_EMAIL} environment variable.}

\item{api_key}{Character. The Global API Key. If \code{NULL} (the
default), reads from the \code{CLOUDFLARE_API_KEY} environment
variable.}
}
\value{
A named list describing the updated record.
}
\description{
Performs a \code{PATCH} on the record, only sending fields supplied
as non-\code{NULL}. Use a \code{PUT}-style replace via \code{\link[=cf_request]{cf_request()}} if
you need to overwrite the entire record.
}
\examples{
\dontshow{if (requireNamespace("vcr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontshow{
if (!nzchar(Sys.getenv("CLOUDFLARE_API_TOKEN"))) {
  Sys.setenv(CLOUDFLARE_API_TOKEN = "cloudflarer-example")
}
vcr::insert_example_cassette(
  "cf_update_dns_record",
  package = "cloudflarer",
  match_requests_on = c("method", "uri")
)
}
cf_update_dns_record("zone-1", "rec-1", content = "192.0.2.2")
\dontshow{vcr::eject_cassette()}
\dontshow{\}) # examplesIf}
}
\seealso{
Other dns: 
\code{\link{cf_create_dns_record}()},
\code{\link{cf_delete_dns_record}()},
\code{\link{cf_get_dns_record}()},
\code{\link{cf_list_dns_records}()}
}
\concept{dns}
