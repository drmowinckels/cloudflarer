% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-analytics-zone.R
\name{cf_zone_requests}
\alias{cf_zone_requests}
\title{Daily or hourly HTTP request totals for a zone}
\usage{
cf_zone_requests(
  zone_id,
  since,
  until,
  by = c("day", "hour"),
  limit = 100L,
  token = NULL,
  email = NULL,
  api_key = NULL
)
}
\arguments{
\item{zone_id}{Character. Cloudflare zone identifier
(\code{zoneTag} in GraphQL).}

\item{since, until}{Date or \code{POSIXct} (or pre-formatted character
\code{"YYYY-MM-DD"} for \code{by = "day"} and \code{"YYYY-MM-DDTHH:MM:SSZ"}
for \code{by = "hour"}). The interval is half-open \verb{[since, until)}.}

\item{by}{Bin width: \code{"day"} or \code{"hour"}.}

\item{limit}{Maximum number of bins to return. Cloudflare caps
the GraphQL query at 10000.}

\item{token}{Character. An API token. If \code{NULL} (the default),
the value of \code{Sys.getenv("CLOUDFLARE_API_TOKEN")} is used.}

\item{email}{Character. Account email. If \code{NULL} (the default),
reads from the \code{CLOUDFLARE_EMAIL} environment variable.}

\item{api_key}{Character. The Global API Key. If \code{NULL} (the
default), reads from the \code{CLOUDFLARE_API_KEY} environment
variable.}
}
\value{
A data.frame with columns:
\code{date} (chr; \code{"YYYY-MM-DD"} for daily, \code{"YYYY-MM-DDTHH:00:00Z"}
for hourly), \code{requests}, \code{bytes}, \code{pageviews}, \code{threats},
\code{uniques}.
}
\description{
Convenience wrapper around the Cloudflare GraphQL Analytics
\code{httpRequests1dGroups} (daily) or \code{httpRequests1hGroups}
(hourly) nodes. Returns a tidy data.frame with one row per time
bin and columns for requests, bytes, page views, threats, and
unique visitors.
}
\examples{
\dontshow{if (requireNamespace("vcr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontshow{
Sys.setenv(CLOUDFLARE_API_TOKEN = "cloudflarer-example")
vcr::insert_example_cassette(
  "cf_zone_requests",
  package = "cloudflarer",
  match_requests_on = c("method", "uri")
)
}
cf_zone_requests(
  "abc123",
  since = Sys.Date() - 7,
  until = Sys.Date(),
  by    = "day"
)

cf_zone_requests(
  "abc123",
  since = Sys.time() - 24 * 3600,
  until = Sys.time(),
  by    = "hour"
)
\dontshow{vcr::eject_cassette()}
\dontshow{\}) # examplesIf}
}
\seealso{
Other analytics: 
\code{\link{cf_cache_ratio}()},
\code{\link{cf_dns_queries}()},
\code{\link{cf_firewall_events_by_day}()},
\code{\link{cf_firewall_events_top}()},
\code{\link{cf_get_rum_site}()},
\code{\link{cf_graphql}()},
\code{\link{cf_list_rum_sites}()},
\code{\link{cf_rum_page_views}()},
\code{\link{cf_rum_top}()},
\code{\link{cf_workers_invocations}()},
\code{\link{cf_zone_overview}()}
}
\concept{analytics}
