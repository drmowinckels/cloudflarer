% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-tunnels.R
\name{cf_list_tunnel_connections}
\alias{cf_list_tunnel_connections}
\title{List active connections for a Cloudflare Tunnel}
\usage{
cf_list_tunnel_connections(
  account_id,
  tunnel_id,
  as_df = TRUE,
  token = NULL,
  email = NULL,
  api_key = NULL
)
}
\arguments{
\item{account_id}{Character. Cloudflare account identifier.}

\item{tunnel_id}{Character. Tunnel identifier.}

\item{as_df}{Logical. When \code{TRUE} (the default), returns a
data.frame via \code{\link[=cf_records_to_df]{cf_records_to_df()}}.}

\item{token}{Character. An API token. If \code{NULL} (the default),
the value of \code{Sys.getenv("CLOUDFLARE_API_TOKEN")} is used.}

\item{email}{Character. Account email. If \code{NULL} (the default),
reads from the \code{CLOUDFLARE_EMAIL} environment variable.}

\item{api_key}{Character. The Global API Key. If \code{NULL} (the
default), reads from the \code{CLOUDFLARE_API_KEY} environment
variable.}
}
\value{
A data.frame of connection records (or list when
\code{as_df = FALSE}).
}
\description{
Returns the currently-connected \code{cloudflared} instances for the
tunnel.
}
\examples{
\dontshow{if (requireNamespace("vcr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontshow{
Sys.setenv(CLOUDFLARE_API_TOKEN = "cloudflarer-example")
vcr::insert_example_cassette("cf_list_tunnel_connections", package = "cloudflarer")
}
cf_list_tunnel_connections("abc123", "tunnel-1")
\dontshow{vcr::eject_cassette()}
\dontshow{\}) # examplesIf}
}
\seealso{
Other tunnels: 
\code{\link{cf_get_tunnel}()},
\code{\link{cf_list_tunnels}()}
}
\concept{tunnels}
