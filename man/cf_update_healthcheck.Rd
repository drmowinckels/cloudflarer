% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-healthchecks.R
\name{cf_update_healthcheck}
\alias{cf_update_healthcheck}
\title{Update a healthcheck}
\usage{
cf_update_healthcheck(
  zone_id,
  healthcheck_id,
  name = NULL,
  address = NULL,
  type = NULL,
  check_regions = NULL,
  http_config = NULL,
  tcp_config = NULL,
  interval = NULL,
  retries = NULL,
  timeout = NULL,
  description = NULL,
  suspended = NULL,
  ...,
  token = NULL,
  email = NULL,
  api_key = NULL
)
}
\arguments{
\item{zone_id}{Character. Cloudflare zone identifier.}

\item{healthcheck_id}{Character. Healthcheck identifier.}

\item{name}{Display name for the healthcheck.}

\item{address}{Hostname or IPv4/IPv6 address to monitor.}

\item{type}{One of \code{"HTTPS"}, \code{"HTTP"}, or \code{"TCP"}.}

\item{check_regions}{Optional character vector of Cloudflare region
codes (\code{"WEU"}, \code{"ENAM"}, etc.). When \code{NULL}, the API runs the
check from all available regions.}

\item{http_config}{Optional named list with HTTP/HTTPS-specific
fields (\code{path}, \code{port}, \code{method}, \code{expected_codes},
\code{expected_body}, \code{follow_redirects}, \code{allow_insecure}, \code{header}).}

\item{tcp_config}{Optional named list with TCP-specific fields
(\code{port}, \code{method}).}

\item{interval}{Polling interval in seconds.}

\item{retries}{Number of retries before marking unhealthy.}

\item{timeout}{Timeout per check, in seconds.}

\item{description}{Optional human-readable description.}

\item{suspended}{Logical. When \code{TRUE}, the check is paused.}

\item{...}{Additional fields forwarded to the API.}

\item{token}{Character. An API token. If \code{NULL} (the default),
the value of \code{Sys.getenv("CLOUDFLARE_API_TOKEN")} is used.}

\item{email}{Character. Account email. If \code{NULL} (the default),
reads from the \code{CLOUDFLARE_EMAIL} environment variable.}

\item{api_key}{Character. The Global API Key. If \code{NULL} (the
default), reads from the \code{CLOUDFLARE_API_KEY} environment
variable.}
}
\value{
A named list describing the updated healthcheck.
}
\description{
Performs a \code{PATCH}, sending only fields supplied as non-\code{NULL}.
}
\examples{
\dontrun{
cf_update_healthcheck("zone-1", "hc-1", suspended = TRUE)
}
}
\seealso{
Other healthchecks: 
\code{\link{cf_create_healthcheck}()},
\code{\link{cf_delete_healthcheck}()},
\code{\link{cf_get_healthcheck}()},
\code{\link{cf_list_healthchecks}()}
}
\concept{healthchecks}
