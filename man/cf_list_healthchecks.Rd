% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-healthchecks.R
\name{cf_list_healthchecks}
\alias{cf_list_healthchecks}
\title{List standalone healthchecks in a zone}
\usage{
cf_list_healthchecks(
  zone_id,
  per_page = 50,
  max_pages = Inf,
  as_df = TRUE,
  token = NULL,
  email = NULL,
  api_key = NULL
)
}
\arguments{
\item{zone_id}{Character. Cloudflare zone identifier.}

\item{per_page, max_pages}{Pagination controls, see
\code{\link[=cf_collect]{cf_collect()}}.}

\item{as_df}{Logical. When \code{TRUE} (the default), returns a
data.frame via \code{\link[=cf_records_to_df]{cf_records_to_df()}}.}

\item{token}{Character. An API token. If \code{NULL} (the default),
the value of \code{Sys.getenv("CLOUDFLARE_API_TOKEN")} is used.}

\item{email}{Character. Account email. If \code{NULL} (the default),
reads from the \code{CLOUDFLARE_EMAIL} environment variable.}

\item{api_key}{Character. The Global API Key. If \code{NULL} (the
default), reads from the \code{CLOUDFLARE_API_KEY} environment
variable.}
}
\value{
A data.frame of healthcheck records (or list when
\code{as_df = FALSE}).
}
\description{
Returns the standalone Healthchecks configured in the zone.
These are separate from load-balancer pool monitors.
}
\examples{
\dontrun{
cf_list_healthchecks("zone-1")
}
}
\seealso{
Other healthchecks: 
\code{\link{cf_create_healthcheck}()},
\code{\link{cf_delete_healthcheck}()},
\code{\link{cf_get_healthcheck}()},
\code{\link{cf_update_healthcheck}()}
}
\concept{healthchecks}
