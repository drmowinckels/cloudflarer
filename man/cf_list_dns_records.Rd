% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-dns-records.R
\name{cf_list_dns_records}
\alias{cf_list_dns_records}
\title{List DNS records in a zone}
\usage{
cf_list_dns_records(
  zone_id,
  type = NULL,
  name = NULL,
  per_page = 50,
  max_pages = Inf,
  as_df = TRUE,
  token = NULL,
  email = NULL,
  api_key = NULL
)
}
\arguments{
\item{zone_id}{Character. Cloudflare zone identifier.}

\item{type}{Optional record type filter, for example \code{"A"} or
\code{"CNAME"}.}

\item{name}{Optional record name filter.}

\item{per_page}{Page size, see \code{\link[=cf_collect]{cf_collect()}}.}

\item{max_pages}{Maximum pages to retrieve, see
\code{\link[=cf_collect]{cf_collect()}}.}

\item{as_df}{Logical. When \code{TRUE} (the default), returns a
data.frame via \code{\link[=cf_records_to_df]{cf_records_to_df()}}. Set to \code{FALSE} for the
raw nested list.}

\item{token}{Character. An API token. If \code{NULL} (the default),
the value of \code{Sys.getenv("CLOUDFLARE_API_TOKEN")} is used.}

\item{email}{Character. Account email. If \code{NULL} (the default),
reads from the \code{CLOUDFLARE_EMAIL} environment variable.}

\item{api_key}{Character. The Global API Key. If \code{NULL} (the
default), reads from the \code{CLOUDFLARE_API_KEY} environment
variable.}
}
\value{
A data.frame of DNS records (or list when
\code{as_df = FALSE}).
}
\description{
List DNS records in a zone
}
\examples{
\dontshow{if (requireNamespace("vcr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontshow{
if (!nzchar(Sys.getenv("CLOUDFLARE_API_TOKEN"))) {
  Sys.setenv(CLOUDFLARE_API_TOKEN = "cloudflarer-example")
}
vcr::insert_example_cassette("cf_list_dns_records", package = "cloudflarer")
}
cf_list_dns_records("abc123")
cf_list_dns_records("abc123", type = "A")
\dontshow{vcr::eject_cassette()}
\dontshow{\}) # examplesIf}
}
\seealso{
Other dns: 
\code{\link{cf_create_dns_record}()},
\code{\link{cf_delete_dns_record}()},
\code{\link{cf_get_dns_record}()},
\code{\link{cf_update_dns_record}()}
}
\concept{dns}
