% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-analytics-firewall.R
\name{cf_firewall_events_top}
\alias{cf_firewall_events_top}
\title{Top firewall events for a zone by a chosen dimension}
\usage{
cf_firewall_events_top(
  zone_id,
  since,
  until,
  dimension = "action",
  limit = 25L,
  token = NULL,
  email = NULL,
  api_key = NULL
)
}
\arguments{
\item{zone_id}{Character. Cloudflare zone identifier
(\code{zoneTag} in GraphQL).}

\item{since, until}{Date or \code{POSIXct}. Half-open \verb{[since, until)}.}

\item{dimension}{Character. Dimension name to group by. Common
choices: \code{"action"} (block, challenge, ...), \code{"source"} (WAF,
firewall rules, security level, ...), \code{"ruleId"},
\code{"clientCountryName"}, \code{"clientRequestPath"},
\code{"clientRequestHTTPHost"}, \code{"userAgent"}.}

\item{limit}{Number of rows to return.}

\item{token}{Character. An API token. If \code{NULL} (the default),
the value of \code{Sys.getenv("CLOUDFLARE_API_TOKEN")} is used.}

\item{email}{Character. Account email. If \code{NULL} (the default),
reads from the \code{CLOUDFLARE_EMAIL} environment variable.}

\item{api_key}{Character. The Global API Key. If \code{NULL} (the
default), reads from the \code{CLOUDFLARE_API_KEY} environment
variable.}
}
\value{
A data.frame with two columns: the requested
\code{dimension} and \code{events} (the event count).
}
\description{
Groups \code{firewallEventsAdaptiveGroups} by a single dimension
and returns the top entries by count.
}
\examples{
\dontshow{if (requireNamespace("vcr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontshow{
Sys.setenv(CLOUDFLARE_API_TOKEN = "cloudflarer-example")
vcr::insert_example_cassette(
  "cf_firewall_events_top",
  package = "cloudflarer",
  match_requests_on = c("method", "uri")
)
}
cf_firewall_events_top(
  "abc123",
  since = Sys.Date() - 7,
  until = Sys.Date(),
  dimension = "action",
  limit = 10
)
\dontshow{vcr::eject_cassette()}
\dontshow{\}) # examplesIf}
}
\seealso{
Other analytics: 
\code{\link{cf_cache_ratio}()},
\code{\link{cf_dns_queries}()},
\code{\link{cf_firewall_events_by_day}()},
\code{\link{cf_get_rum_site}()},
\code{\link{cf_graphql}()},
\code{\link{cf_list_rum_sites}()},
\code{\link{cf_rum_page_views}()},
\code{\link{cf_rum_top}()},
\code{\link{cf_workers_invocations}()},
\code{\link{cf_zone_overview}()},
\code{\link{cf_zone_requests}()}
}
\concept{analytics}
