% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-overview.R
\name{cf_zone_overview}
\alias{cf_zone_overview}
\title{One-call summary of a zone's recent activity}
\usage{
cf_zone_overview(
  zone_id,
  since = Sys.Date() - 7,
  until = Sys.Date(),
  account_id = NULL,
  site_tag = NULL,
  token = NULL,
  email = NULL,
  api_key = NULL
)
}
\arguments{
\item{zone_id}{Character. Cloudflare zone identifier.}

\item{since, until}{Date or \code{POSIXct}. Half-open \verb{[since, until)}.
Defaults to the last 7 days.}

\item{account_id}{Optional account identifier. Required for the
\code{top_countries} slot.}

\item{site_tag}{Optional Web Analytics site tag. Required for
the \code{top_countries} slot.}

\item{token}{Character. An API token. If \code{NULL} (the default),
the value of \code{Sys.getenv("CLOUDFLARE_API_TOKEN")} is used.}

\item{email}{Character. Account email. If \code{NULL} (the default),
reads from the \code{CLOUDFLARE_EMAIL} environment variable.}

\item{api_key}{Character. The Global API Key. If \code{NULL} (the
default), reads from the \code{CLOUDFLARE_API_KEY} environment
variable.}
}
\value{
A named list with class \code{cloudflarer_overview}:
\itemize{
\item \code{traffic} -- daily requests, bytes, page views, threats,
uniques (from \code{\link[=cf_zone_requests]{cf_zone_requests()}}).
\item \code{cache} -- daily cache hit ratios (from \code{\link[=cf_cache_ratio]{cf_cache_ratio()}}).
\item \code{dns} -- daily DNS query counts (from \code{\link[=cf_dns_queries]{cf_dns_queries()}}).
\item \code{firewall} -- daily firewall event counts (from
\code{\link[=cf_firewall_events_by_day]{cf_firewall_events_by_day()}}; Pro+ plans only).
\item \code{top_countries} -- top countries by RUM page views (from
\code{\link[=cf_rum_top]{cf_rum_top()}}); only populated when \code{account_id} and
\code{site_tag} are supplied.
\item \code{summary} -- a one-row data.frame with the period totals.
}
}
\description{
Bundles the most-used analytics endpoints into a single call
and returns a named list of tidy data.frames covering traffic,
cache effectiveness, DNS queries, firewall events, and (when
\code{site_tag} is supplied) Web Analytics top countries.
}
\details{
Each underlying call is wrapped in \code{tryCatch()}, so an
individual failure (Free-plan gating, missing permission,
empty data) yields \code{NULL} for that component instead of
aborting the whole overview.
}
\examples{
\dontrun{
ov <- cf_zone_overview(zone_id, account_id = account_id, site_tag = site_tag)
ov$traffic
ov$summary
}
}
\seealso{
Other analytics: 
\code{\link{cf_cache_ratio}()},
\code{\link{cf_dns_queries}()},
\code{\link{cf_firewall_events_by_day}()},
\code{\link{cf_firewall_events_top}()},
\code{\link{cf_get_rum_site}()},
\code{\link{cf_graphql}()},
\code{\link{cf_list_rum_sites}()},
\code{\link{cf_rum_page_views}()},
\code{\link{cf_rum_top}()},
\code{\link{cf_workers_invocations}()},
\code{\link{cf_zone_requests}()}
}
\concept{analytics}
