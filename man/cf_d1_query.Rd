% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-d1.R
\name{cf_d1_query}
\alias{cf_d1_query}
\title{Run a SQL query against a D1 database}
\usage{
cf_d1_query(
  account_id,
  database_id,
  sql,
  params = NULL,
  as_df = TRUE,
  token = NULL,
  email = NULL,
  api_key = NULL
)
}
\arguments{
\item{account_id}{Character. Cloudflare account identifier.}

\item{database_id}{Character. D1 database \code{uuid}.}

\item{sql}{Character. SQL statement(s) to execute.}

\item{params}{Optional character vector of positional parameters
bound to the \verb{?} placeholders in \code{sql}.}

\item{as_df}{Logical. When \code{TRUE} (the default), returns the first
result set as a data.frame via \code{\link[=cf_records_to_df]{cf_records_to_df()}}. Set to
\code{FALSE} to get the full raw response (useful for multi-statement
queries or when you need the metadata).}

\item{token}{Character. An API token. If \code{NULL} (the default),
the value of \code{Sys.getenv("CLOUDFLARE_API_TOKEN")} is used.}

\item{email}{Character. Account email. If \code{NULL} (the default),
reads from the \code{CLOUDFLARE_EMAIL} environment variable.}

\item{api_key}{Character. The Global API Key. If \code{NULL} (the
default), reads from the \code{CLOUDFLARE_API_KEY} environment
variable.}
}
\value{
A data.frame of result rows when \code{as_df = TRUE}, otherwise
the raw response (a list of result blocks, each with \code{success},
\code{meta}, and \code{results}).
}
\description{
Executes one or more SQL statements against the database. Use
\code{params} for parameterised queries to avoid string-interpolation
issues.
}
\examples{
\dontrun{
cf_d1_query("acc-1", "db-1", "SELECT * FROM users WHERE id = ?", params = "42")
}
}
\seealso{
Other d1: 
\code{\link{cf_create_d1_database}()},
\code{\link{cf_delete_d1_database}()},
\code{\link{cf_get_d1_database}()},
\code{\link{cf_list_d1_databases}()}
}
\concept{d1}
