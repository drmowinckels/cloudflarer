% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-request.R
\name{cf_collect}
\alias{cf_collect}
\title{Collect every page of a paginated endpoint}
\usage{
cf_collect(req, per_page = 50, max_pages = Inf, ...)
}
\arguments{
\item{req}{An \code{httr2_request} from \code{\link[=cf_request]{cf_request()}}.}

\item{per_page}{Integer page size. Cloudflare caps most endpoints
at 50; some allow up to 1000.}

\item{max_pages}{Optional integer. Stop after collecting this
many pages. Useful for exploratory calls against large
accounts. \code{Inf} (the default) collects everything.}

\item{...}{Additional arguments passed to \code{\link[httr2:req_perform]{httr2::req_perform()}}.}
}
\value{
A list of records.
}
\description{
Given a request built with \code{\link[=cf_request]{cf_request()}} (optionally with query
filters piped on), walks every page of a Cloudflare list endpoint
and concatenates the \code{result} arrays into a single list. Page
size and paging cursor are added as query parameters on each
request.
}
\examples{
\dontshow{if (requireNamespace("vcr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontshow{
Sys.setenv(CLOUDFLARE_API_TOKEN = "cloudflarer-example")
vcr::insert_example_cassette("cf_collect", package = "cloudflarer")
}
cf_request("zones") |> cf_collect(per_page = 50)
\dontshow{vcr::eject_cassette()}
\dontshow{\}) # examplesIf}
}
\seealso{
Other requests: 
\code{\link{cf_request}()},
\code{\link{cf_resp}()}
}
\concept{requests}
