% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-email-routing.R
\name{cf_list_email_routing_addresses}
\alias{cf_list_email_routing_addresses}
\title{List Email Routing destination addresses}
\usage{
cf_list_email_routing_addresses(
  account_id,
  verified_only = FALSE,
  per_page = 50,
  max_pages = Inf,
  as_df = TRUE,
  token = NULL,
  email = NULL,
  api_key = NULL
)
}
\arguments{
\item{account_id}{Character. Cloudflare account identifier.}

\item{verified_only}{Logical. When \code{TRUE}, asks the API to
return only verified addresses.}

\item{per_page, max_pages}{Pagination controls.}

\item{as_df}{Logical. See \code{\link[=cf_list_email_routing_rules]{cf_list_email_routing_rules()}}.}

\item{token}{Character. An API token. If \code{NULL} (the default),
the value of \code{Sys.getenv("CLOUDFLARE_API_TOKEN")} is used.}

\item{email}{Character. Account email. If \code{NULL} (the default),
reads from the \code{CLOUDFLARE_EMAIL} environment variable.}

\item{api_key}{Character. The Global API Key. If \code{NULL} (the
default), reads from the \code{CLOUDFLARE_API_KEY} environment
variable.}
}
\value{
A data.frame of destination addresses (or list when
\code{as_df = FALSE}).
}
\description{
Returns the verified destination addresses available for
routing in the account. Destinations are account-scoped, not
zone-scoped, because the same destination can be used across
multiple routed zones.
}
\examples{
\dontshow{if (requireNamespace("vcr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontshow{
Sys.setenv(CLOUDFLARE_API_TOKEN = "cloudflarer-example")
vcr::insert_example_cassette(
  "cf_list_email_routing_addresses",
  package = "cloudflarer"
)
}
cf_list_email_routing_addresses("abc123")
\dontshow{vcr::eject_cassette()}
\dontshow{\}) # examplesIf}
}
\seealso{
Other email: 
\code{\link{cf_get_email_routing_settings}()},
\code{\link{cf_list_email_routing_rules}()}
}
\concept{email}
