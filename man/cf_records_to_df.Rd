% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-utils.R
\name{cf_records_to_df}
\alias{cf_records_to_df}
\title{Convert a list of Cloudflare records to a data.frame}
\usage{
cf_records_to_df(records)
}
\arguments{
\item{records}{A list of named lists with broadly consistent
field names. Missing fields become \code{NA}.}
}
\value{
A data.frame with one row per record and columns named
by the union of field names. Empty input returns an empty
data.frame.
}
\description{
Helper that takes a list of named records (typically the \code{result}
array from a Cloudflare REST endpoint) and returns a data.frame
with one row per record. Scalar fields become typed columns;
vector- or object-valued fields become list-columns.
}
\examples{
records <- list(
  list(id = "a", name = "alpha", tags = c("x", "y")),
  list(id = "b", name = "beta",  tags = character(0))
)
cf_records_to_df(records)
}
\concept{helpers}
