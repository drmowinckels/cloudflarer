% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-page-rules.R
\name{cf_list_page_rules}
\alias{cf_list_page_rules}
\title{List Page Rules for a zone}
\usage{
cf_list_page_rules(
  zone_id,
  status = NULL,
  order = NULL,
  direction = NULL,
  as_df = TRUE,
  token = NULL,
  email = NULL,
  api_key = NULL
)
}
\arguments{
\item{zone_id}{Character. Cloudflare zone identifier.}

\item{status}{Optional status filter (\code{"active"} or
\code{"disabled"}).}

\item{order}{Optional ordering, see the Cloudflare API
reference. For example \code{"priority"} or \code{"status"}.}

\item{direction}{Optional sort direction, \code{"asc"} or
\code{"desc"}.}

\item{as_df}{Logical. When \code{TRUE} (the default), returns a
data.frame via \code{\link[=cf_records_to_df]{cf_records_to_df()}}. Set to \code{FALSE} for the
raw nested list.}

\item{token}{Character. An API token. If \code{NULL} (the default),
the value of \code{Sys.getenv("CLOUDFLARE_API_TOKEN")} is used.}

\item{email}{Character. Account email. If \code{NULL} (the default),
reads from the \code{CLOUDFLARE_EMAIL} environment variable.}

\item{api_key}{Character. The Global API Key. If \code{NULL} (the
default), reads from the \code{CLOUDFLARE_API_KEY} environment
variable.}
}
\value{
A data.frame of page rules (or list when
\code{as_df = FALSE}). Each rule's \code{targets} and \code{actions} are
kept as list-columns because they are themselves arrays of
nested objects.
}
\description{
Returns the Page Rules configured for a zone (URL-pattern based
overrides for cache, SSL, security, redirects, etc.).
}
\examples{
\dontshow{if (requireNamespace("vcr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontshow{
Sys.setenv(CLOUDFLARE_API_TOKEN = "cloudflarer-example")
vcr::insert_example_cassette("cf_list_page_rules", package = "cloudflarer")
}
cf_list_page_rules("abc123")
\dontshow{vcr::eject_cassette()}
\dontshow{\}) # examplesIf}
}
\seealso{
Other zones: 
\code{\link{cf_create_page_rule}()},
\code{\link{cf_delete_page_rule}()},
\code{\link{cf_get_page_rule}()},
\code{\link{cf_get_zone}()},
\code{\link{cf_get_zone_setting}()},
\code{\link{cf_get_zone_settings}()},
\code{\link{cf_list_firewall_rules}()},
\code{\link{cf_list_zones}()},
\code{\link{cf_page_rule_action}()},
\code{\link{cf_page_rule_target}()},
\code{\link{cf_update_page_rule}()}
}
\concept{zones}
