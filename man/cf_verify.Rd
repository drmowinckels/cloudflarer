% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-auth.R
\name{cf_verify}
\alias{cf_verify}
\alias{cf_token_verify}
\title{Verify the active Cloudflare credential}
\usage{
cf_verify(token = NULL, email = NULL, api_key = NULL)

cf_token_verify(token = NULL)
}
\arguments{
\item{token}{Character. An API token. If \code{NULL} (the default),
the value of \code{Sys.getenv("CLOUDFLARE_API_TOKEN")} is used.}

\item{email}{Character. Account email. If \code{NULL} (the default),
reads from the \code{CLOUDFLARE_EMAIL} environment variable.}

\item{api_key}{Character. The Global API Key. If \code{NULL} (the
default), reads from the \code{CLOUDFLARE_API_KEY} environment
variable.}
}
\value{
A named list. The shape depends on the auth mode.
}
\description{
For token auth, calls \verb{/user/tokens/verify} and returns the token
record. For Global API Key auth, calls \verb{/user} and returns the
user record (the verify endpoint is token-only). Either way, a
successful return means the credential authenticates against the
API.
}
\examples{
\dontrun{
cf_verify()
}
}
\seealso{
Other authentication: 
\code{\link{cf_api_key}()},
\code{\link{cf_auth_mode}()},
\code{\link{cf_email}()},
\code{\link{cf_has_auth}()},
\code{\link{cf_sitrep}()},
\code{\link{cf_token}()}
}
\concept{authentication}
