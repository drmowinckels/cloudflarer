% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-workers-kv.R
\name{cf_list_kv_keys}
\alias{cf_list_kv_keys}
\title{List the keys in a Workers KV namespace}
\usage{
cf_list_kv_keys(
  account_id,
  namespace_id,
  prefix = NULL,
  limit = 1000,
  max_pages = Inf,
  as_df = TRUE,
  token = NULL,
  email = NULL,
  api_key = NULL
)
}
\arguments{
\item{account_id}{Character. Cloudflare account identifier.}

\item{namespace_id}{Character. KV namespace identifier.}

\item{prefix}{Optional character. Restrict results to keys
starting with this prefix.}

\item{limit}{Integer. Page size, between 10 and 1000.}

\item{max_pages}{Optional integer. Stop after collecting this
many pages. \code{Inf} (the default) collects every key.}

\item{as_df}{Logical. When \code{TRUE} (the default), returns a
data.frame via \code{\link[=cf_records_to_df]{cf_records_to_df()}}. Set to \code{FALSE} for the
raw nested list.}

\item{token}{Character. An API token. If \code{NULL} (the default),
the value of \code{Sys.getenv("CLOUDFLARE_API_TOKEN")} is used.}

\item{email}{Character. Account email. If \code{NULL} (the default),
reads from the \code{CLOUDFLARE_EMAIL} environment variable.}

\item{api_key}{Character. The Global API Key. If \code{NULL} (the
default), reads from the \code{CLOUDFLARE_API_KEY} environment
variable.}
}
\value{
A data.frame of key records (or list when
\code{as_df = FALSE}), each with \code{name}, \code{expiration}, and
\code{metadata} (when set).
}
\description{
Walks the namespace's cursor-paginated \code{keys} endpoint and
concatenates every page into a single result. Unlike the
offset-paginated list endpoints wrapped by \code{\link[=cf_collect]{cf_collect()}},
Cloudflare paginates KV keys with an opaque cursor, so this
wrapper keeps its own loop instead.
}
\examples{
\dontshow{if (requireNamespace("vcr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontshow{
if (!nzchar(Sys.getenv("CLOUDFLARE_API_TOKEN"))) {
  Sys.setenv(CLOUDFLARE_API_TOKEN = "cloudflarer-example")
}
vcr::insert_example_cassette("cf_list_kv_keys", package = "cloudflarer")
}
cf_list_kv_keys("abc123", "ns-1")
\dontshow{vcr::eject_cassette()}
\dontshow{\}) # examplesIf}
}
\seealso{
Other workers: 
\code{\link{cf_create_kv_namespace}()},
\code{\link{cf_delete_kv_namespace}()},
\code{\link{cf_delete_kv_value}()},
\code{\link{cf_delete_kv_values}()},
\code{\link{cf_get_kv_namespace}()},
\code{\link{cf_get_kv_value}()},
\code{\link{cf_get_workers_script}()},
\code{\link{cf_list_kv_namespaces}()},
\code{\link{cf_list_workers_scripts}()},
\code{\link{cf_put_kv_value}()},
\code{\link{cf_put_kv_values}()},
\code{\link{cf_rename_kv_namespace}()},
\code{\link{cf_workers_invocations}()}
}
\concept{workers}
