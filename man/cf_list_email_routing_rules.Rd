% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-email-routing.R
\name{cf_list_email_routing_rules}
\alias{cf_list_email_routing_rules}
\title{List Email Routing rules for a zone}
\usage{
cf_list_email_routing_rules(
  zone_id,
  enabled_only = FALSE,
  per_page = 50,
  max_pages = Inf,
  as_df = TRUE,
  token = NULL,
  email = NULL,
  api_key = NULL
)
}
\arguments{
\item{zone_id}{Character. Cloudflare zone identifier.}

\item{enabled_only}{Logical. When \code{TRUE}, asks the API to
return only enabled rules.}

\item{per_page, max_pages}{Pagination controls, see
\code{\link[=cf_collect]{cf_collect()}}.}

\item{as_df}{Logical. When \code{TRUE} (the default), returns a
data.frame via \code{\link[=cf_records_to_df]{cf_records_to_df()}}.}

\item{token}{Character. An API token. If \code{NULL} (the default),
the value of \code{Sys.getenv("CLOUDFLARE_API_TOKEN")} is used.}

\item{email}{Character. Account email. If \code{NULL} (the default),
reads from the \code{CLOUDFLARE_EMAIL} environment variable.}

\item{api_key}{Character. The Global API Key. If \code{NULL} (the
default), reads from the \code{CLOUDFLARE_API_KEY} environment
variable.}
}
\value{
A data.frame of routing rules (or list when
\code{as_df = FALSE}).
}
\description{
Returns the address-matching rules that decide how incoming
emails are forwarded.
}
\examples{
\dontshow{if (requireNamespace("vcr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontshow{
if (!nzchar(Sys.getenv("CLOUDFLARE_API_TOKEN"))) {
  Sys.setenv(CLOUDFLARE_API_TOKEN = "cloudflarer-example")
}
vcr::insert_example_cassette(
  "cf_list_email_routing_rules",
  package = "cloudflarer"
)
}
cf_list_email_routing_rules("abc123")
\dontshow{vcr::eject_cassette()}
\dontshow{\}) # examplesIf}
}
\seealso{
Other email: 
\code{\link{cf_get_email_routing_settings}()},
\code{\link{cf_list_email_routing_addresses}()}
}
\concept{email}
