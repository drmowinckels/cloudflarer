% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-cache.R
\name{cf_purge_cache}
\alias{cf_purge_cache}
\title{Purge zone cache}
\usage{
cf_purge_cache(
  zone_id,
  files = NULL,
  hosts = NULL,
  prefixes = NULL,
  tags = NULL,
  purge_everything = FALSE,
  token = NULL,
  email = NULL,
  api_key = NULL
)
}
\arguments{
\item{zone_id}{Character. Cloudflare zone identifier.}

\item{files}{Optional character vector of URLs (or list of
\code{list(url = ..., headers = ...)} for advanced purges with
custom request headers).}

\item{hosts}{Optional character vector of hostnames whose cached
content should be invalidated.}

\item{prefixes}{Optional character vector of URL prefixes
(without scheme) to invalidate, e.g. \code{"example.com/blog"}.}

\item{tags}{Optional character vector of cache tags (Enterprise
plan).}

\item{purge_everything}{Logical. When \code{TRUE}, ignores the
targeted arguments and purges the entire zone cache.}

\item{token}{Character. An API token. If \code{NULL} (the default),
the value of \code{Sys.getenv("CLOUDFLARE_API_TOKEN")} is used.}

\item{email}{Character. Account email. If \code{NULL} (the default),
reads from the \code{CLOUDFLARE_EMAIL} environment variable.}

\item{api_key}{Character. The Global API Key. If \code{NULL} (the
default), reads from the \code{CLOUDFLARE_API_KEY} environment
variable.}
}
\value{
A named list with the purge job \code{id}.
}
\description{
Tells Cloudflare to drop cached content for a zone. Pass one
of the targeted-purge arguments to scope the purge, or set
\code{purge_everything = TRUE} to wipe the entire zone cache.
}
\details{
Targeted purges are strictly preferred to wipes because cache
wipes can briefly increase origin load. Cloudflare also rate
limits the wipe endpoint per zone.
}
\examples{
\dontshow{if (requireNamespace("vcr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontshow{
if (!nzchar(Sys.getenv("CLOUDFLARE_API_TOKEN"))) {
  Sys.setenv(CLOUDFLARE_API_TOKEN = "cloudflarer-example")
}
vcr::insert_example_cassette(
  "cf_purge_cache",
  package = "cloudflarer",
  match_requests_on = c("method", "uri")
)
}
cf_purge_cache("abc123", files = c(
  "https://example.com/index.html",
  "https://example.com/style.css"
))

cf_purge_cache("abc123", purge_everything = TRUE)
\dontshow{vcr::eject_cassette()}
\dontshow{\}) # examplesIf}
}
\concept{cache}
