% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-graphql.R
\name{cf_graphql}
\alias{cf_graphql}
\title{Run a Cloudflare GraphQL Analytics query}
\usage{
cf_graphql(
  query,
  ...,
  .operation_name = NULL,
  .token = NULL,
  .email = NULL,
  .api_key = NULL,
  .envir = parent.frame()
)
}
\arguments{
\item{query}{Character. A GraphQL query string.}

\item{...}{Named GraphQL variables. For example
\code{cf_graphql(query, zoneTag = "abc", limit = 10)}.}

\item{.operation_name}{Optional operation name when the query
contains multiple operations.}

\item{.token, .email, .api_key}{Optional per-call credentials.
Dot-prefixed to avoid clashing with GraphQL variable names.
See \code{\link[=cf_token]{cf_token()}}, \code{\link[=cf_email]{cf_email()}}, \code{\link[=cf_api_key]{cf_api_key()}}.}

\item{.envir}{Environment for error reporting. Defaults to the
caller, so error messages point at the user's code.}
}
\value{
The parsed response body, including \code{data} and
(when present) \code{extensions}. Any non-empty \code{errors} array
raises a classed \code{cloudflarer_error}.
}
\description{
Cloudflare's \href{https://developers.cloudflare.com/analytics/graphql-api/}{Analytics GraphQL API}
exposes most current and historical analytics datasets (HTTP
requests, DNS, firewall, Workers, R2, etc.) behind a single
endpoint: \code{POST /graphql}. The response shape is the standard
GraphQL \verb{\{data, errors\}} envelope, not Cloudflare's REST
envelope, so this function is a separate path from
\code{\link[=cf_request]{cf_request()}}.
}
\details{
Variables are passed through \code{...} as named arguments, the same
pattern used by the meetupr package. All values must be named;
an unnamed value raises an error before the request is made.
}
\examples{
\dontrun{
res <- cf_graphql(
  "query Viewer($accountTag: String!) {
     viewer {
       accounts(filter: { accountTag: $accountTag }) {
         accountTag
       }
     }
   }",
  accountTag = "abc123"
)
res$data$viewer$accounts

res <- cf_graphql(
  "
  query ZoneRequests($zoneTag: String!, $since: Date!, $until: Date!) {
    viewer {
      zones(filter: { zoneTag: $zoneTag }) {
        httpRequests1dGroups(
          limit: 100,
          filter: { date_geq: $since, date_lt: $until }
        ) {
          dimensions { date }
          sum { requests bytes pageViews }
          uniq { uniques }
        }
      }
    }
  }
  ",
  zoneTag = "abc123",
  since = "2026-05-01",
  until = "2026-05-08"
)
}
}
\seealso{
Other analytics: 
\code{\link{cf_cache_ratio}()},
\code{\link{cf_dns_queries}()},
\code{\link{cf_firewall_events_by_day}()},
\code{\link{cf_firewall_events_top}()},
\code{\link{cf_get_rum_site}()},
\code{\link{cf_list_rum_sites}()},
\code{\link{cf_rum_page_views}()},
\code{\link{cf_rum_top}()},
\code{\link{cf_workers_invocations}()},
\code{\link{cf_zone_overview}()},
\code{\link{cf_zone_requests}()}
}
\concept{analytics}
