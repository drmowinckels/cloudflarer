% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-rulesets.R
\name{cf_list_account_rulesets}
\alias{cf_list_account_rulesets}
\title{List Rulesets for an account}
\usage{
cf_list_account_rulesets(
  account_id,
  as_df = TRUE,
  token = NULL,
  email = NULL,
  api_key = NULL
)
}
\arguments{
\item{account_id}{Character. Cloudflare account identifier.}

\item{as_df}{Logical. When \code{TRUE} (the default), returns a
data.frame via \code{\link[=cf_records_to_df]{cf_records_to_df()}}. Set to \code{FALSE} for the
raw nested list.}

\item{token}{Character. An API token. If \code{NULL} (the default),
the value of \code{Sys.getenv("CLOUDFLARE_API_TOKEN")} is used.}

\item{email}{Character. Account email. If \code{NULL} (the default),
reads from the \code{CLOUDFLARE_EMAIL} environment variable.}

\item{api_key}{Character. The Global API Key. If \code{NULL} (the
default), reads from the \code{CLOUDFLARE_API_KEY} environment
variable.}
}
\value{
A data.frame of rulesets (or list when
\code{as_df = FALSE}).
}
\description{
Returns the rulesets configured at the account level (typically
the curated managed rulesets you can deploy with overrides).
}
\examples{
\dontshow{if (requireNamespace("vcr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontshow{
if (!nzchar(Sys.getenv("CLOUDFLARE_API_TOKEN"))) {
  Sys.setenv(CLOUDFLARE_API_TOKEN = "cloudflarer-example")
}
vcr::insert_example_cassette("cf_list_account_rulesets", package = "cloudflarer")
}
cf_list_account_rulesets("acc-1")
\dontshow{vcr::eject_cassette()}
\dontshow{\}) # examplesIf}
}
\seealso{
Other rulesets: 
\code{\link{cf_get_account_ruleset}()},
\code{\link{cf_get_ruleset}()},
\code{\link{cf_list_rulesets}()}
}
\concept{rulesets}
