% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-analytics-dns.R
\name{cf_dns_queries}
\alias{cf_dns_queries}
\title{Daily DNS query counts for a zone}
\usage{
cf_dns_queries(
  zone_id,
  since,
  until,
  limit = 100L,
  token = NULL,
  email = NULL,
  api_key = NULL
)
}
\arguments{
\item{zone_id}{Character. Cloudflare zone identifier
(\code{zoneTag} in GraphQL).}

\item{since, until}{Date or \code{POSIXct}. Half-open \verb{[since, until)}.}

\item{limit}{Maximum number of rows. Cloudflare caps the
underlying query at 10000.}

\item{token}{Character. An API token. If \code{NULL} (the default),
the value of \code{Sys.getenv("CLOUDFLARE_API_TOKEN")} is used.}

\item{email}{Character. Account email. If \code{NULL} (the default),
reads from the \code{CLOUDFLARE_EMAIL} environment variable.}

\item{api_key}{Character. The Global API Key. If \code{NULL} (the
default), reads from the \code{CLOUDFLARE_API_KEY} environment
variable.}
}
\value{
A data.frame with columns \code{date} (chr) and \code{queries}
(int).
}
\description{
Wraps the Cloudflare GraphQL \code{dnsAnalyticsAdaptiveGroups} node,
grouped by day. Returns a tidy data.frame.
}
\details{
Requires an API token with the \verb{Account Analytics: Read} (or
\verb{Zone Analytics: Read}) permission. The legacy Global API Key
has full access.
}
\examples{
\dontshow{if (requireNamespace("vcr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontshow{
Sys.setenv(CLOUDFLARE_API_TOKEN = "cloudflarer-example")
vcr::insert_example_cassette(
  "cf_dns_queries",
  package = "cloudflarer",
  match_requests_on = c("method", "uri")
)
}
cf_dns_queries(
  "abc123",
  since = Sys.Date() - 7,
  until = Sys.Date()
)
\dontshow{vcr::eject_cassette()}
\dontshow{\}) # examplesIf}
}
\seealso{
Other analytics: 
\code{\link{cf_cache_ratio}()},
\code{\link{cf_firewall_events_by_day}()},
\code{\link{cf_firewall_events_top}()},
\code{\link{cf_get_rum_site}()},
\code{\link{cf_graphql}()},
\code{\link{cf_list_rum_sites}()},
\code{\link{cf_rum_page_views}()},
\code{\link{cf_rum_top}()},
\code{\link{cf_workers_invocations}()},
\code{\link{cf_zone_overview}()},
\code{\link{cf_zone_requests}()}
}
\concept{analytics}
