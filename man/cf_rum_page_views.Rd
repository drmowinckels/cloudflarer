% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-analytics-rum.R
\name{cf_rum_page_views}
\alias{cf_rum_page_views}
\title{Daily page views for a Web Analytics (RUM) site}
\usage{
cf_rum_page_views(
  account_id,
  site_tag,
  since,
  until,
  limit = 100L,
  token = NULL,
  email = NULL,
  api_key = NULL
)
}
\arguments{
\item{account_id}{Character. Cloudflare account identifier
(\code{accountTag} in GraphQL).}

\item{site_tag}{Character. RUM site tag, as returned by
\code{\link[=cf_list_rum_sites]{cf_list_rum_sites()}}.}

\item{since, until}{Date or \code{POSIXct} (or pre-formatted ISO-8601
character strings). Half-open \verb{[since, until)} interval.}

\item{limit}{Maximum number of days to return. Cloudflare caps
the underlying query at 10000.}

\item{token}{Character. An API token. If \code{NULL} (the default),
the value of \code{Sys.getenv("CLOUDFLARE_API_TOKEN")} is used.}

\item{email}{Character. Account email. If \code{NULL} (the default),
reads from the \code{CLOUDFLARE_EMAIL} environment variable.}

\item{api_key}{Character. The Global API Key. If \code{NULL} (the
default), reads from the \code{CLOUDFLARE_API_KEY} environment
variable.}
}
\value{
A data.frame with one row per day and columns
\code{date} (chr) and \code{pageviews} (int).
}
\description{
Convenience wrapper around the GraphQL
\code{rumPageloadEventsAdaptiveGroups} node with a daily
(\code{date_DAY}) dimension. Returns a tidy data.frame.
}
\examples{
\dontshow{if (requireNamespace("vcr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontshow{
Sys.setenv(CLOUDFLARE_API_TOKEN = "cloudflarer-example")
vcr::insert_example_cassette(
  "cf_rum_page_views",
  package = "cloudflarer",
  match_requests_on = c("method", "uri")
)
}
cf_rum_page_views(
  "acc-1",
  site_tag = "abc",
  since = Sys.Date() - 30,
  until = Sys.Date()
)
\dontshow{vcr::eject_cassette()}
\dontshow{\}) # examplesIf}
}
\seealso{
Other analytics: 
\code{\link{cf_cache_ratio}()},
\code{\link{cf_dns_queries}()},
\code{\link{cf_firewall_events_by_day}()},
\code{\link{cf_firewall_events_top}()},
\code{\link{cf_get_rum_site}()},
\code{\link{cf_graphql}()},
\code{\link{cf_list_rum_sites}()},
\code{\link{cf_rum_top}()},
\code{\link{cf_workers_invocations}()},
\code{\link{cf_zone_overview}()},
\code{\link{cf_zone_requests}()}
}
\concept{analytics}
